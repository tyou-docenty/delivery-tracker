{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ],
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm run build-with-deps",
  "startCommand": "pnpm start",
  "env": {
    "NODE_ENV": "production"
  }
}
